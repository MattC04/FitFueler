import { invalidRedirectException } from '../../../../errors/constants.mjs';

// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
/** @internal */
function getRedirectUrl(redirects) {
    const redirectUrl = redirects?.find(redirect => !redirect.startsWith('http://') && !redirect.startsWith('https://'));
    if (!redirectUrl) {
        throw invalidRedirectException;
    }
    return redirectUrl;
}

export { getRedirectUrl };
//# sourceMappingURL=getRedirectUrl.native.mjs.map
